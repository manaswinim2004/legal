[project]
name = "legallens"
version = "0.1.0"
description = "AI-powered legal contract analysis application"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "torch",
    "transformers",
]